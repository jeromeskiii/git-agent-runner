[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["agent_runner"]

[project]
name = "git-agent-runner"
version = "0.1.0"
description = "All-in-one orchestrator that vendors git-worktree-runner and pr-agent behind one agent-runner CLI"
requires-python = ">=3.12,<3.15"
dependencies = [
    "rich>=13.0.0",
    "pr-agent",
]

[tool.uv.sources]
pr-agent = { path = "vendor/pr-agent", editable = true }

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-cov>=5.0",
    "ruff>=0.4",
    "mypy>=1.10",
]

[project.scripts]
agent-runner = "agent_runner.cli:main"

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP", "B", "C4", "SIM", "RUF"]
ignore = ["E501"]  # line-length handled by formatter

[tool.ruff.lint.per-file-ignores]
"agent_runner/tests/*" = ["B", "SIM"]

[tool.pytest.ini_options]
minversion = "8.0"
testpaths = ["agent_runner/tests"]
addopts = [
    "-ra",
    "--strict-markers",
    "--strict-config",
]
filterwarnings = [
    "error",
]

[tool.mypy]
python_version = "3.12"
strict = true
files = ["agent_runner"]
ignore_missing_imports = true
warn_unreachable = true
warn_unused_ignores = true
disallow_untyped_defs = true
show_error_codes = true

[[tool.mypy.overrides]]
module = "agent_runner.tests.*"
disallow_untyped_defs = false
